image: archlinux
arch: x86_64 
secrets:
 - a0395b1d-ae64-4fb0-8e54-4e044948f0d0 
packages:
 - qemu-user-static
 - binfmt-qemu-static
sources:
 - https://github.com/dukeify/build-images
tasks:
 - configure-host: |
    cd build-images
    sudo ./scripts/setup-host.sh linux
 - build-images: |
    cd build-images
    sudo ./scripts/build-images.sh linux
 - publish-images: |
    cd build-images
    sudo ./scripts/publish-images.sh linux
